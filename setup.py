from setuptools import setup, find_packages

setup(
    name='audience_metrics',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    author='Your Name',
    description='Simple metrics for audience data',
    license='MIT',
    keywords='databricks analytics metrics',
)
